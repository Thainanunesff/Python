#Crie um jogo de adivinhação com o computador, ao acertar qual foi o número escolhido informe quantas tentativas o jogador fez até acertar.
from random import randint
cont = 0
jogador = 0
computador = randint(0,10)
Jogador = int(input('Qual numero eu pensei? '))
while jogador != computador:
    Jogador = int(input('Qual numero eu pensei? '))
    cont += 1
    print(f'Eu pensei em {computador}. Voce errou {cont} vez.')
